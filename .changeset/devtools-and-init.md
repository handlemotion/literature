---
"@handlemotion/literature": minor
"@handlemotion/literature-cli": minor
---

Mount devtools with `<Literature />` from `@handlemotion/literature/devtools` — no separate devtools component file.

`literature init` auto-detects apps in monorepos, supports Vite, and adds `--dry-run` and `--app-dir`.
